syntax = "proto3";

package trpc.test.unittest;

service Greeter {
  // Demo for asynchronous response
  rpc SayHello (HelloRequest) returns (HelloReply) {}
}

message HelloRequest {
   string msg = 1;
}

message HelloReply {
   string msg = 1;
}
